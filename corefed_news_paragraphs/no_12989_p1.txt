If our feeds are ordered by algorithm , this effectively means that Twitter , rather than users , decides which tweets should be seen .Tweets that the algorithm decides are " uninteresting " to users might not be seen at all .Needless to say , I am already receiving DMs from fans claiming that my tweets are not showing up in their feeds .my tweets the # RIPTwitter user revolt demonstrated , an algorithmic timeline is not exactly in demand with Twitter ’s core fans .